<%- include("header") -%>
                    <!-- Content Row -->
                    <div class="row">
                        <!-- Earnings (Monthly) Card Example -->
                        <% for(var i=0;i<reviewsArray.length;i++) { %>
                        <div class="col-lg-12">
                            <!-- Basic Card Example -->
                            <div class="card shadow mb-4">
                                <div class="card-header py-3">
                                    <h6 class="m-0 font-weight-bold text-primary">Username : <%=  reviewsArray[i].username%></h6>
                                </div>
                                <div class="card-body">
                                    <h6>Review : </h6>
                                    <%= reviewsArray[i].review %>
                                </div>
                            </div>
                    <!-- Content Row -->
                        </div>
                        <% } %>
                <!-- /.container-fluid -->
                    </div>
            <!-- End of Main Content -->
<%- include("footer") -%>